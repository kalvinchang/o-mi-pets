<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.goodluckexe.topag">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".StoreScreen" />
        <activity android:name=".PetSelectionScreen" />
        <activity android:name=".PlayerStatsScreen" />
        <activity android:name=".SettingsScreen" />
        <activity android:name=".MainActivity" />
        <activity android:name=".WorkoutScreen" />
        <activity android:name=".tutorialScreen"></activity>
    </application>

</manifest>